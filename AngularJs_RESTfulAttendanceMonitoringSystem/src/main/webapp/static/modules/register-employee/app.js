var app = angular.module('register-employee-module', []);


/**
 * Form data can be retrieved even after submitting the form
 * Store the form_name in $rootScope inside the run() method.
 * @param $rootScope
 * @returns
 */
app.run(function($rootScope) {
    //$rootScope.myForm = {};
})
/*
app.config(function ($qProvider) {
    $qProvider.errorOnUnhandledRejections(false);
});
*/